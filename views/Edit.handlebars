<h2 id="">Bienvenid@ {{user.nombre}}</h2>
<div class>
  <h2>Datos del perfil</h2>
  <hr />

  <form>
    <div class="form-group row w-50 m-auto">
      <div class="form-group col-12 col-sm-6">
        <label>Email</label>
        <input class="form-control m-auto" disabled value={{user.email}} />
      </div>
      <div class="form-group col-12 col-sm-6">
        <label>Nombre</label>
        <input class="form-control m-auto" value="{{user.nombre}}" />
      </div>
      <div class="form-group col-12 col-sm-6">
        <label>Nueva Password</label>
        <input type="password" class="form-control m-auto" value="" />
      </div>
      <div class="form-group col-12 col-sm-6">
        <label>Repita la password</label>
        <input type="password" class="form-control m-auto" value="" />
      </div>
      <div class="form-group col-12 col-sm-6">
        <label>Años de experiencia</label>
        <input class="form-control m-auto" value="{{user.anios_experiencia}}" />
      </div>
      <div class="form-group col-12 col-sm-6">
        <label>Especialidad</label>
        <input class="form-control m-auto" value="{{user.especialidad}}" />
      </div>
    </div>
    <div class="mb-1">
      <button class="btn btn-primary my-2" id="updateBtn">Actualizar</button>
    </div>
    <div>
      <button class="btn btn-danger" id="deleteBtn">Eliminar cuenta</button>
    </div>
  </form>
</div>

<script src="./js/edit.js"></script>